{# The comment below is to prevent edits to the rendered workflow file. Feel free to edit the template file. -#}
# DO NOT EDIT THIS FILE
#
# This workflow is generated from:
# Template: https://github.com/per1234/github-actions-workflow-resources/blob/master/templates/spell-check.yml.j2
# Data file in this repository: .github/workflows/data/spell-check_data.yml
#
# Edits to either of those files will trigger an automated update to the workflow.

name: Spell Check

on:
  - push
  - pull_request

jobs:
  spellcheck:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Spell check
        uses: arduino/actions/libraries/spell-check@master
        with:
{%- if jobs["spellcheck"]["steps"]["spell-check"]["with"]["ignore-words-list"] is defined %}
          ignore-words-list: {{ jobs["spellcheck"]["steps"]["spell-check"]["with"]["ignore-words-list"] }}
{%- endif %}
{%- if jobs["spellcheck"]["steps"]["spell-check"]["with"]["skip-paths"] is defined %}
          skip-paths: {{ jobs["spellcheck"]["steps"]["spell-check"]["with"]["skip-paths"] }}
{%- endif %}
